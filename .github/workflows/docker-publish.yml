name: Docker Image Build

on:
  push:
    branches:
      - main

env:
  # TODO: Change variable to your image's name.
  IMAGE_NAME: image

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: docker build indexer
        run: |
          docker build . -t indexing
